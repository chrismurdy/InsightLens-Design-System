Standard labelled text field. Square corners, uppercase tracked label above.

\`\`\`jsx
<Input label="Work email" name="email" placeholder="you@company.com" required />
\`\`\`

Pass \`error\` to switch the border and helper text to danger red; \`hint\` for
neutral helper copy; \`icon="search"\` to prefix a Material glyph.
