Pill prompt field with a round blue send button — the one deliberately round
element in the system, taken from the platform's DISCOVER agent screen.

\`\`\`jsx
<PromptField value={q} onChange={e => setQ(e.target.value)} onSubmit={ask}
  placeholder="Show me the Alberta branch" />
\`\`\`
